<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.teamand.bookstore">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:vmSafeMode="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.LoginActivity"></activity>
        <activity android:name=".ui.activity.RegisterActivity"
            android:parentActivityName=".ui.activity.LoginActivity"></activity>
        <activity android:name=".ui.activity.SearchActivity"/>
        <activity android:name=".ui.activity.ProfileUserActivity"/>
        <activity android:name=".ui.activity.ListBookActivity"/>
        <activity android:name=".ui.activity.BookDetailActivity"/>
        <activity android:name=".ui.activity.WishListActivity"/>
        <activity android:name=".ui.activity.CartActivity"/>
        <activity android:name=".ui.activity.CheckoutActivity"/>
        <activity android:name=".ui.activity.ShippingAddressActivity"/>
        <activity android:name=".ui.activity.SettingAccountActivity"/>
        <activity android:name=".ui.activity.OrdersActivity"/>
    </application>

</manifest>